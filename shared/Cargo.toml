[package]
name = "shared"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0"
serde = { version = "1.0.228", features = ["derive"] }

[dependencies.uuid]
version = "1.22.0"
features = [
    "v4",
]
